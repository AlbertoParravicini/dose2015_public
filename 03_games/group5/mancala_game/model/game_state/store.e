note
	description: "Represents a store."
	author: ""
	date: "$Date$"
	revision: "$Revision$"

class
	STORE
inherit
	BASKET
feature
	--add_stones(stones: INTEGER)
		-- Adds more than one stone to the basket
end
